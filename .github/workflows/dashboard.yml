name: dashboard ci

on:
  push:
    branches: [ master, pre_*, ft_*, release/* ]
  pull_request:
    branches: [ master, pre_*, ft_*, release/* ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.7"]
        os: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
        cache-dependency-path: 'src/dashboard/apigateway/requirements_dev.txt'

    - name: Install dependencies
      run: pip install -r src/dashboard/apigateway/requirements_dev.txt -i https://pypi.org/simple/

    - name: Lint
      run: cd src/dashboard && make edition-ee && make lint

    - name: Test
      run:  cd src/dashboard && make edition-ee && make test