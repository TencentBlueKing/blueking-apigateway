name: core-api

on:
  push:
    branches: [ master, pre_*, ft_*, release/* ]
  pull_request:
    branches: [ master, pre_*, ft_*, release/* ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.20'
        cache-dependency-path: src/core-api/go.sum

    - name: Build
      run: cd src/core-api && make build

    - name: golangci-lint
      uses: golangci/golangci-lint-action@v3
      with:
        version: v1.29
        working-directory: src/core-api

    - name: Test
      run: cd src/core-api && make test