### 描述

取消待审批的 MCP Server 权限申请。仅 `pending` 状态的记录可以取消。

### 输入参数

#### 路径参数

| 参数名称  | 参数类型 | 必选 | 描述        |
|-----------|----------|------|-------------|
| record_id | int      | 是   | 申请记录 ID |

#### 请求参数

| 参数名称        | 参数类型 | 必选 | 描述           |
|-----------------|----------|------|----------------|
| target_app_code | string   | 是   | 申请权限的应用 |
| operated_by     | string   | 是   | 受信调用方声明的操作人，最长 32 个字符；仅用于展示和审计，不作为鉴权依据 |

### 响应

取消成功返回 HTTP 204。
