meta {
  name: no-header
  type: http
  seq: 2
}

get {
  url: {{scheme}}://{{host}}/api/smoke/prod/bk-username-required
  body: none
  auth: none
}

assert {
  res.status: eq 400
  res.body["code"]: eq 1640001
  res.body["code_name"]: eq INVALID_ARGS
  res.body["message"]: contains "No `X-Bk-Username` header or no `bk_username` in the `X-Bkapi-Authorization` header"
}
